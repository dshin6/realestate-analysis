# Real Estate Analysis Agent Rules

## Operational Commands

Python과 `requirements.txt`를 사용한다.

- 가상환경 생성: `python -m venv .venv`
- Linux/macOS 활성화: `source .venv/bin/activate`
- 의존성 설치: `python -m pip install -r requirements.txt`
- 앱 실행: `streamlit run app.py`
- 테스트: `python -m unittest discover -s tests -v`
- 실제 API 키는 `.streamlit/secrets.toml`에만 저장하고 커밋하지 않는다.

## Required Context

작업을 시작하기 전에 작업 범위에 필요한 문서만 읽는다.

1. 목표나 요구사항을 다루면 `memory-bank/project-brief.md`
2. 구현 작업이면 `memory-bank/implementation-plan.md`
3. 이어서 작업하면 `memory-bank/progress.md`
4. 구조에 영향을 주면 `memory-bank/architecture.md`
5. 기존 선택의 이유가 필요하면 `memory-bank/decisions.md`

모든 문서를 습관적으로 다시 읽지 않는다. 작은 작업은 관련 문서만 확인한다.

## Development Mode

이 프로젝트의 기본 목표는 빠른 피드백을 통해 바이브 코딩에 익숙해지는 것이다.
절차의 완전성보다 작은 결과를 빠르게 만들고 확인하는 것을 우선하되, 복구하기 어려운 위험은 별도로 관리한다.

### Quick

문구, 스타일, 작은 설정, 단순한 문서 변경처럼 영향 범위가 작고 쉽게 되돌릴 수 있는 작업이다.

- 긴 인터뷰나 별도 설계 문서를 만들지 않는다.
- 요청을 이해할 수 있으면 바로 수정한다.
- 변경한 부분과 가장 가까운 검증만 수행한다.
- 구조나 다음 작업에 영향이 없으면 memory-bank 갱신을 생략할 수 있다.

### Standard

일반 기능, 여러 파일에 걸친 변경, 일반적인 버그 수정에 사용한다.

- 모호한 핵심 조건만 짧게 확인한다.
- `implementation-plan.md`에 간결한 작업 순서를 기록한다.
- 작은 단위로 구현하고 관련 테스트 또는 직접 실행으로 검증한다.
- 완료 후 `progress.md`를 갱신한다.

### High-risk

데이터 삭제·변환, 보안, 인증, 결제, 배포, 큰 구조 변경, 복구하기 어려운 작업에 사용한다.

- 구현 전에 범위, 위험, 복구 방법과 완료 조건을 명시한다.
- 필요한 경우 상세 설계와 테스트 계획을 작성한다.
- 파괴적 작업 전 대상을 읽기 전용 방식으로 확인한다.
- 전체 관련 검증을 통과한 뒤 완료로 기록한다.

판단이 애매하면 Standard를 사용한다. Superpowers식 전체 절차는 사용자가 요청하거나 High-risk 작업에 실질적으로 필요한 경우에만 적용한다.

## Golden Rules

### Immutable

- 사용자 데이터와 원본 자료를 명시적 승인 없이 삭제하거나 덮어쓰지 않는다.
- 비밀번호, API 키, 개인정보와 민감한 부동산 자료를 코드, 문서, 로그에 기록하지 않는다.
- 분석 결과와 원본 데이터, 사실과 추정, 확인된 값과 가정을 명확히 구분한다.
- 사용자가 요청하지 않은 배포, 외부 메시지 전송, 결제 또는 공개 공유를 수행하지 않는다.
- 작업 중 발견한 사용자 변경사항을 임의로 되돌리지 않는다.

### Do

- 모호함이 결과를 크게 바꾸지 않으면 합리적인 가정을 밝히고 진행한다.
- 큰 기능을 실행하고 확인할 수 있는 작은 단위로 나눈다.
- 버그는 가능한 경우 먼저 재현하고 원인을 확인한다.
- 실제 코드와 문서가 달라지면 현재 동작을 확인하고 문서 갱신을 제안한다.
- 검증한 사실만 완료했다고 보고한다.

### Don't

- 단순 작업에 장문의 계획과 불필요한 문서를 추가하지 않는다.
- 미래에 필요할 것이라는 이유만으로 기능, 추상화 또는 의존성을 미리 추가하지 않는다.
- 데이터 출처, 계산식과 주요 가정을 숨기지 않는다.
- 테스트를 통과시키기 위해 요구사항이나 검증 기준을 약화하지 않는다.
- 생성물, 의존성 폴더와 임시 파일을 소스처럼 관리하지 않는다.

## Project Context

부동산 관련 데이터를 분석해 사용자의 판단을 돕는 프로젝트다. 구체적인 사용자, 분석 범위와 결과물은 인터뷰를 통해 `memory-bank/project-brief.md`에 확정한다.

Tech Stack: Python, Streamlit. 프로젝트 초기화 전이므로 버전과 보조 라이브러리는 아직 확정하지 않았다.

## Working Agreement

- 한 번에 질문 하나를 우선하며 초보자가 결정할 수 있도록 선택지와 차이를 짧게 설명한다.
- 사용자가 명확히 요청한 범위 안에서는 확인 질문을 최소화하고 직접 진행한다.
- 구현 중 선택이 필요하면 가장 단순한 안을 기본값으로 추천한다.
- 진행 상황 보고는 결과, 확인 방법, 다음 선택 순으로 간결하게 작성한다.
- 새 기능은 현재 목표에 필요한 최소 범위로 시작하고 사용해 본 뒤 확장한다.

## Memory Bank Maintenance

- `project-brief.md`: 목표, 사용자, 범위 또는 완료 조건이 바뀔 때 갱신한다.
- `implementation-plan.md`: Standard 이상 작업을 시작하거나 작업 순서가 바뀔 때 갱신한다.
- `progress.md`: 의미 있는 기능 또는 마일스톤을 검증한 뒤 갱신한다.
- `architecture.md`: 실제 구조, 데이터 흐름 또는 주요 인터페이스가 바뀔 때만 갱신한다.
- `decisions.md`: 되돌리기 어렵거나 이후 작업에 영향을 주는 선택만 기록한다.
- 문서에는 계획과 실제 상태를 섞지 않고 날짜와 상태를 명시한다.

## Standards and References

- 코딩 스타일과 파일 규칙은 기술 스택이 정해진 뒤 실제 도구 설정을 기준으로 추가한다.
- 커밋을 사용하는 경우 `feat:`, `fix:`, `docs:`, `test:`, `refactor:`, `chore:` 형식을 권장한다.
- 한 커밋에는 가능한 한 하나의 논리적 변경을 담는다.
- 규칙이 개발 속도를 반복적으로 방해하면 삭제 또는 완화를 제안한다.
- 규칙과 실제 프로젝트가 어긋나면 조용히 무시하지 말고 `AGENTS.md` 갱신을 제안한다.
