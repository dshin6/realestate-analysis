# Development Progress

마지막 갱신: 2026-07-29

## Current Milestone

첫 번째 Streamlit 대시보드와 실제 전체 기간 데이터 수집·렌더링 검증을 완료했다.

## Completed

- Quick, Standard, High-risk 작업 등급을 정의했다.
- Superpowers 전체 절차를 기본으로 강제하지 않는 속도 우선 원칙을 정했다.
- 프로젝트 목표, 계획, 진행, 구조와 의사결정을 분리해 기록할 memory-bank 골격을 만들었다.
- Claude Code가 동일한 규칙을 읽도록 `CLAUDE.md`를 연결했다.
- 첫 기능의 데이터는 사용자가 제공하는 파일이 아니라 인터넷 또는 API에서 자동 수집하기로 했다.
- 국토교통부 실거래가 API와 한국부동산원 공동주택 식별정보 API를 공식 데이터 후보로 확인했다.
- 첫 분석 대상을 반송동 21의 `동탄시범한빛마을한화꿈에그린`으로 확정했다.
- 호갱노노에서 이 단지의 거래별 109A/B/C 평면 타입 표시를 확인했다.
- 사용자는 A 타입을 선호하며 현재 검토 중인 특정 매물은 없다.
- 분석 기간은 단지 입주 시점인 2007년부터 확보 가능한 최신 거래까지로 정했다.
- 타입별 가격 프리미엄과 거래량 변화를 선호도 변화의 대리 지표로 보기로 했다.
- 전체 기간은 연도별로 요약하고 거래가 충분한 구간은 분기별로 확대하기로 했다.
- 실제 층은 거래마다 표시하고 통계는 1층·저층(2~5층)·중층(6~15층)·고층(16층 이상)으로 묶기로 했다.
- 첫 결과물은 Python Streamlit 기반 로컬 웹 대시보드로 만들기로 했다.
- 첫 버전은 `동탄시범한빛마을한화꿈에그린` 단일 단지 전용으로 만들기로 했다.
- 이후에는 사용자가 요청하는 한빛마을 단지를 하나씩 추가하고 전국 검색은 당분간 제외하기로 했다.
- 공공데이터포털 회원가입을 완료했다.
- 국토교통부 실거래가 API 활용신청을 완료했다.
- API XML 파서, 월별 수집, 대상 단지 필터와 로컬 캐시를 구현했다.
- 전용면적 기반 A/B/C 타입 매핑과 실제 층 기준 고정 구간 분류를 구현했다.
- 연도별 타입 중앙값, 단순 프리미엄과 최근 3년 매물가 비교 계산을 구현했다.
- Streamlit 단일 화면과 표본 XML·분석 계산 단위 테스트를 추가했다.
- 연도별 기본 추세와 분기별 상세 추세, 동·타입별 비교 차트를 구현했다.
- 실제 API로 2007-07-24부터 2026-07-04까지 정상 거래 615건을 수집했다.
- 실데이터의 A/B/C 분류 결과는 각각 315/190/110건이며 면적 미분류 거래는 없었다.
- 실데이터로 핵심 지표 4개, Plotly 차트 5개와 거래표 1개의 렌더링을 확인했다.
- 공개 GitHub 저장소와 Streamlit Community Cloud 배포를 완료했다.
- 공개 실거래 615건을 빠른 첫 화면용 초기 데이터로 분리했다.
- 신규 월만 증분 수집하고 수동 새로고침은 최근 3개월만 재조회하도록 개선했다.
- 동 정보가 미공개인 거래도 실제 층으로 분류되도록 층 구간을 단순화했다.
- Streamlit 세션에 남은 이전 층 구간명을 매 실행마다 다시 계산하도록 수정했다.

## In Progress

- 배포된 대시보드의 실제 사용성 확인

## Verification Evidence

- Windows `.venv`에서 단위·렌더링 테스트 7개 통과(2026-07-29).
- 임시 캐시로 Plotly 차트 5개와 거래표 렌더링 경로를 확인했다.
- 키 미설정 초기 화면은 예외 0건으로 렌더링됐고 Streamlit 서버 시작을 확인했다.
- 실제 캐시 615건의 전체 화면 렌더링에서 예외 0건을 확인했다.
- Community Cloud 배포 문서 갱신 후 단위·렌더링 테스트 7개를 다시 통과했다.
- 공개 대상 파일 검사에서 `.streamlit/secrets.toml`, `data/cache/`, `.venv/`, `.vendor/`, `.tools/`가 제외됨을 확인했다.
- 초기 데이터·증분 수집·화면 렌더링을 포함한 테스트 10개를 통과했다.
- 고정 층 구간 변경 후 테스트 10개를 통과했고 실데이터 615건 모두 분류됨을 확인했다.
- 층 구간별 거래는 1층 22건, 저층 67건, 중층 276건, 고층 250건이다.
- 오래된 세션 상태 회귀 테스트를 포함한 전체 테스트 11개를 통과했다.

## Known Limitations

- 동 정보 공개 거래는 615건 중 95건이라 동별 가격 비교의 대부분이 `미공개`로 묶인다.
- 타입 프리미엄은 연도별 단지 중앙값만 보정한 단순 지표이며 동·층·거래시점 효과를 모두 통제하지 않는다.

## Next Step

배포된 대시보드에서 초기 화면이 즉시 열리고 수동 새로고침이 최근 3개월만 조회하는지 확인한다.
